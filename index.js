function add() {
    // we'll fill this in shortly
  }
  function subtract() {
    // we'll fill this in shortly
  }
  function multiply() {
    // we'll fill this in shortly
  }
  function divide() {
    // we'll fill this in shortly
  
}function add(a,b) {
    return a+b;
     // we'll fill this in shortly
  }
  function subtract(a,b) {
    return a-b;
    // we'll fill this in shortly
  }
  function multiply(a,b) {
    return a*b;
    // we'll fill this in shortly
  }
  function divide(a,b) {
    return a/b;
    // we'll fill this in shortly
  }
  function increment(n) {
    return ++n;
  }
function decrement(n) {
    return --n;
}
function makeInt(n) {
   return parseInt(n,10);
}
function preserveDecimal(n) {
    if (parseFloat(n,10)==NaN){return NaN;}
    return parseFloat(n,10);
 }